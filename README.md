# docker-condalock-jupyterlab

- `make cl`: update the conda-lock.yml file after you update the `environment.yml`
- `make build`: re-build the docker image to install the new packages from conda-lock
- `make up`: run the container

- `make env`: installs the env on your local machine
